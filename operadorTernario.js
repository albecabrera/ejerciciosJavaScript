let usuario = "Emilia";

let autorizado = usuario === "Emilia" ? 'Autorizado' : 'No autorizado';
console.log(autorizado);

// Ejercicio para el condicional ternario
let edad = 12;
let puedeConducir = edad >= 18 ? 'Puede conducir' : 'No puede conducir';
console.log(puedeConducir);

// Ejercicio para practicar el operador ternario
// Determina si un número es par o impar usando un operador ternario
let numero = 6;
let numeroImpar = numero % 2 === 1 ? 'El número es impar' : 'El número es par';
console.log(numeroImpar);

let nombre = "Guido"
let permiso = nombre === "Guido" ? "Puede entrar" : "No tiene acceso ninguno";
console.log(permiso);


let fitnessStudio = "Telekom Baskets"
let entryCard = fitnessStudio === "Telekom Baskets" ? "Tarjeta de acceso directo" : "Entrada denegada";
console.log(entryCard);