// declarar funciones 
let inicial = 3;

function sumar() {
    inicial += 4;
    return inicial;
}
console.log(sumar());
console.log(sumar());
console.log(sumar());

// Otro ejemplo de función 
 let puertaAbierta = true; 
    while (puertaAbierta) { 
        console.log("La puerta está abierta");
     } // Simulamos que alguien la cierra puertaAbierta = false; }

